fn is_prime(n) {
	if n == 1 || n == 2 => true;

	for i := 2; i < n / 2; i+=1 {
		if n % i == 0 => false;
	}

	return true;
}

search_region := 30000;
found := 0;

for i := 1; i < search_region; i+=1 ->
	if is_prime(i) -> found += 1;

println("primes: ", found / search_region, "%");
