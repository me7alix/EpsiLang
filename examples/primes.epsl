fn is_prime(n) {
	if n == 1 or n == 2 => true;

	for i := 2; i < n / 2; i+=1 {
		if n % i == 0 => false;
	}

	return true;
}

region := 30000;
found := 0;

for i := 1; i < region; i+=1 do
	if is_prime(i) do found += 1;

println("primes: ", found / region, "%");
